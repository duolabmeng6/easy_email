# Email API 接口文档

## 1. 获取邮件列表

**接口地址**: `/api/email/list`

**请求方式**: GET

**请求参数**:
- `recipient`: 收件人邮箱地址 (必填)

**响应格式**: JSON

**响应内容**: 返回指定收件人的所有邮件列表，按创建时间降序排列

## 2. 获取邮件详情

**接口地址**: `/api/email/get`

**请求方式**: GET

**请求参数**:
- `id`: 邮件ID (必填)

**响应格式**: JSON

**响应内容**: 返回指定ID的邮件详细信息 